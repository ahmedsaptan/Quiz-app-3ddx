{{#each quizzes}}
  <div class="card card-body mb-2">
    <h4>Quiz title: {{title}}</h4>
    <p>Details: {{details}}</p>
    <p>Created By {{byName}}</p>
    <a href="/quizzes/{{id}}" class="btn btn-primary btn-block"> View Quiz </a>
  </div>

{{else}}
  <p>No Quizes Listed</p>
{{/each}}